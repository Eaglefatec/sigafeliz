package com.sigafeliz.model;

// Enums são classes que definem um conjunto de constantes que podem ser usadas como dados.
// Nesse caso, são usadas porque há uma coluna de uma tabela com um conjunto de opções selecionáveis e fixas.
public enum Prioridade {
    ALTA,
    MEDIA,
    BAIXA;
}